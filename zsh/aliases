# Writing
alias vim="nvim"

# General
alias whatismyip="dig -4 TXT +short o-o.myaddr.l.google.com @ns1.google.com"
alias cdr='cd $(git rev-parse --show-cdup)'


# Go
alias bt="go build ./... && go test -coverprofile=.coverage.out ./..."
alias cv="go tool cover -html=.coverage.out"
